/* CTRL + N para executar o código */
function exercicio1(x, y) {
  let soma = x + y
  let subtracao = x - y
  let multiplicacao = x * y
  let divisao = x / y
  return `Soma: ${soma}, Subtração: ${subtracao}, Multiplicação: ${multiplicacao}, Divisão: ${divisao}`;
  }
  
console.log(exercicio1(2,3))